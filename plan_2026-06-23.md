# plan — 2026-06-23

Honest post-fix-pass plan. After this session shipped:
onboarding paste-CV, jobs hide/save, URL persist, word-boundary skill match,
scrape_filters validation, server-side hide filter, deferred URL sync,
skills split into proficiency table. 34/34 vitest pass.

Daily-use blockers still standing. Ordered by ratio of (user value) / (effort).

---

## P0 — blocks daily use

### 1. Match-score breakdown popover  (½ day)
- Click `82` badge → popover: "4 skills hit (Python ×lvl4, FastAPI ×lvl3, Postgres ×lvl3, Docker ×lvl2), 2 missed (Kubernetes, Redis)".
- Reuses existing `matchedSkills` from `jobs.list` response — just expose the missed set too.
- Why: scores without explanation = noise. Trust unlock.

### 2. Tailored CV bullets per job + 1-click PDF  (2 days)
- New action `cv.tailor({ jobId })` → Gemini prompt with job description + current bullets → returns rewritten bullets.
- UI on `/jobs` card: "Tailor CV for this" button → diff view → accept → save as `cv_drafts` row → PDF download.
- The moat. Without this app is "another bookmarker."

### 3. Kanban pipeline drag-drop  (1 day)
- `/apply` page: columns Applied / Replied / Interview / Offer / Reject.
- `applications.update({ status })` already exists. Need dnd-kit wiring + column lanes.
- Why: tracker board is the second most-asked workflow in `addFeature.md`.

### 4. Follow-up reminder cron + Telegram ping  (½ day)
- Convex cron: every morning, find applications with `status="applied"` and `applied_at < now-7d`.
- Send Telegram brief "follow up on X at Y, applied 7 days ago".
- Telegram action wiring already in `convex/telegram.ts`.
- Why: app must reach out, not wait for user to open.

---

## P1 — quality / retention

### 5. Real Playwright tests for the new flows  (½ day)
- Hide → reload → still hidden, not in default view, in hidden tab.
- Save → reload → still saved, in saved tab.
- Paste CV (small fixture) → step advances → step 2 shows skill count.
- Filter chips reload → URL params restore.

### 6. Match-score upgrade — alias DB  (1 day)
- Replace hardcoded alias map with seeded `skill_aliases` table (~200 entries: AWS↔Amazon Web Services, K8s↔Kubernetes, etc.).
- Editable via settings so user can teach it.

### 7. Mobile pass  (1 day)
- Replace hover-cards with bottom-sheets on `< md` breakpoint.
- Jobs list: collapse 3-col → 1-col already works. Verify tap targets ≥ 44px.

### 8. Daily focus block + email digest  (1 day, blocked on email infra)
- Resend integration (or use existing Telegram path as MVP).
- 1 milestone + 1 resource + top 3 jobs ≥ 80 score, daily 08:00.

---

## P2 — polish

- Drop confetti from learn-page milestone completion (item from `newPlan` stop-doing list, still firing).
- Cmd-K palette already exists; verify it covers jobs + skills + apps.
- "Export all data" 1-click ZIP — single Convex action that walks user's rows.
- Settings: "Redo onboarding" button → `/welcome?force=1` (gate already supports it).

## P3 — explicitly NOT doing now

- Local-only mode (no Convex) — architectural fork, not justified for solo user.
- Mock interview AI — separate product, deserves its own scope.
- Offline-first note sync — weeks of work, low daily-use payoff vs P0 items.

## Testing gaps to close alongside

- jobs.list `view=hidden|saved` filter test (server-side).
- cv.parseText auth + size-limit test (mock Gemini fetch).
- Playwright run in CI on PR.

## Definition of "fully shipped"

User can: sign up → paste CV → see 50 ranked jobs → click "tailor CV" →
download tailored PDF → mark applied → app reminds at day 7 → user updates
to "interview" by dragging. All on mobile or desktop. No console errors.
